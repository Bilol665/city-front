
server:
    port: 8085

spring:
    application:
        name: FRONT-END

    datasource:
        username: postgres
        password: 1402
        url: jdbc:postgresql://localhost:5433/city_front
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
    mail:
        host: smtp.gmail.com
        port: 587
        username: rishatovabdulaziz101@gmail.com
        password: clhtlbqqacigdnxw
        properties:
            mjaail:
                smtp:
                    auth: true
            mail:
                smtp:
                    starttls:
                        enable: true

services:
    user-service: http://localhost:8080/user

